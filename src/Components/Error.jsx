import { NavLink } from "react-router-dom";


const Error = () => {
    return (
        <div>
            <NavLink className="btn" to="/">Back</NavLink>
        </div>
    );
};

export default Error;